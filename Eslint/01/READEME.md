### Eslint初体验

#### 1. 使用 npm 安装 ESLint
  ```
  npm install eslint --save-dev
  ```

#### 2. 初始化项目生成package.json文件
  ```
  npm init
  ```

#### 3. 设置eslint配置文件(eslintrc.js)
  ```
  ./node_modules/.bin/eslint --init
  ```
  > 输入命令后，会在命令行选择一些简单的使用规则： 比如分号，三双引号的使用规定。
